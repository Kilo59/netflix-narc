name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  static-analysis:
    name: Static Analysis
    strategy:
      matrix:
        task: ["lint --check", "fmt --check", "type-check --check"]
      fail-fast: false
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Set up Python
        run: uv python install 3.13

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Run ${{ matrix.task }}
        run: uv run invoke ${{ matrix.task }}

  tests:
    name: Tests
    strategy:
      fail-fast: ${{ github.event.pull_request.draft == true }}
      matrix:
        python-version: ["3.13", "3.14"]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Run tests
        run: uv run invoke test

  pre-publish:
    name: Test package installation & binary build
    needs: [static-analysis, tests]
    if: >-
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'pull_request' && github.event.pull_request.draft == false)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Set up Python
        run: uv python install 3.13

      - name: Validate pyproject metadata
        run: uvx --with "validate-pyproject[all]" validate-pyproject pyproject.toml

      - name: Build package
        run: uv build

      - name: Test Standalone Binary Build via PyApp
        env:
          PYAPP_EMBED: "1"
        run: uv run --only-group build invoke build-binary
